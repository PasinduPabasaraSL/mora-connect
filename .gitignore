# User-uploaded content — keep the directory structure and the rules that stop
# Apache executing what lands in it, ignore the uploads themselves
uploads/*
!uploads/.gitkeep
!uploads/.htaccess
!uploads/avatars/
uploads/avatars/*
!uploads/avatars/.gitkeep

# Local environment overrides — the template stays tracked so the list of
# settings is documented, but never the file holding the real values
.env
.env.*
!.env.example
config/database.local.php

# PHP / Composer (not used yet, but harmless to pre-empt)
/vendor/
composer.phar
*.log

# Editors and IDEs
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~

# OS cruft
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# Database dumps and backups (database.sql is the tracked schema, keep it)
*.sql.gz
*.bak
*.tmp

.cursor